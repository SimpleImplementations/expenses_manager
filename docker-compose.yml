services:
  app:
    build:
      context: .
      target: prod
    restart: unless-stopped
    env_file: .env
    environment:
      DB_PATH: /var/lib/bot/bot.db
    ports:
      - "8080:8080"
    volumes:
      - /srv/botdata:/var/lib/bot
